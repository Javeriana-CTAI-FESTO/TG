# Configuracion del servidor
server:
  port: ${SERVER_PORT:8080}

# Configuracion de spring
spring:
  # Configuración del acceso a datos
  datasource:
    url: jdbc:ucanaccess://${DATABASE_FILE:c:/users/nbayo/downloads/tg/tg/database.accdb};openExclusive=false;ignoreCase=true
    driver-class-name: net.ucanaccess.jdbc.UcanaccessDriver